{# Introduction Section #}
<div class="air-card air-intro-card">
    <div class="air-card-body">
        <div class="air-intro-content">
            <div class="air-intro-icon">
                <span class="dashicons dashicons-format-image"></span>
            </div>
            <div class="air-intro-text">
                <h2 class="air-intro-title">
                    {{ __('Welcome to AI Image Renamer', 'ai-image-renamer') }}
                </h2>
                <p class="air-intro-description">
                    {{ __('Automatically rename newly uploaded images with short, descriptive filenames based on their actual content. You can also reuse the generated description as alt text for better accessibility.', 'ai-image-renamer') }}
                    <strong>{{ __('This version connects to Groq only.', 'ai-image-renamer') }}</strong>
                </p>
                <h3 class="air-intro-subtitle">{{ __('Quick Start Guide', 'ai-image-renamer') }}</h3>
                <div class="air-intro-steps">
                    <div class="air-step">
                        <div class="air-step-indicator">1</div>
                        <div class="air-step-content">
                            <span class="air-step-title">{{ __('Connect API', 'ai-image-renamer') }}</span>
                            <span class="air-step-desc">{{ __('Enter your Groq API key and test the connection first', 'ai-image-renamer') }}</span>
                        </div>
                    </div>
                    <div class="air-step-separator"></div>
                    <div class="air-step">
                        <div class="air-step-indicator">2</div>
                        <div class="air-step-content">
                            <span class="air-step-title">{{ __('Configure', 'ai-image-renamer') }}</span>
                            <span class="air-step-desc">{{ __('Choose your AI model, file types, and optional alt text behavior', 'ai-image-renamer') }}</span>
                        </div>
                    </div>
                    <div class="air-step-separator"></div>
                    <div class="air-step">
                        <div class="air-step-indicator">3</div>
                        <div class="air-step-content">
                            <span class="air-step-title">{{ __('Upload', 'ai-image-renamer') }}</span>
                            <span class="air-step-desc">{{ __('Upload images in Media → Library or through the block editor', 'ai-image-renamer') }}</span>
                        </div>
                    </div>
                    <div class="air-step-separator"></div>
                    <div class="air-step">
                        <div class="air-step-indicator">4</div>
                        <div class="air-step-content">
                            <span class="air-step-title">{{ __('Fallback', 'ai-image-renamer') }}</span>
                            <span class="air-step-desc">
                                {{ __('If the API request fails, WordPress keeps the original filename so uploads can still complete.', 'ai-image-renamer') }}
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
