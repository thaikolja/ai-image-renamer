{# Tab Navigation Component #}
<nav
        class="air-tabs"
        role="tablist">
    <button
            type="button"
            class="air-tab active"
            data-tab="api"
            role="tab"
            aria-selected="true"
            aria-controls="air-panel-api">
        <span class="dashicons dashicons-admin-network"></span>
        <span class="air-tab-label">{{ __('API', 'ai-image-renamer') }}</span>
    </button>
    <button
            type="button"
            class="air-tab"
            data-tab="general"
            role="tab"
            aria-selected="false"
            aria-controls="air-panel-general">
        <span class="dashicons dashicons-admin-settings"></span>
        <span class="air-tab-label">{{ __('General', 'ai-image-renamer') }}</span>
    </button>
    <button
            type="button"
            class="air-tab"
            data-tab="filetypes"
            role="tab"
            aria-selected="false"
            aria-controls="air-panel-filetypes">
        <span class="dashicons dashicons-format-image"></span>
        <span class="air-tab-label">{{ __('File Types', 'ai-image-renamer') }}</span>
    </button>
    <button
            type="button"
            class="air-tab"
            data-tab="advanced"
            role="tab"
            aria-selected="false"
            aria-controls="air-panel-advanced">
        <span class="dashicons dashicons-admin-tools"></span>
        <span class="air-tab-label">{{ __('Advanced', 'ai-image-renamer') }}</span>
    </button>
</nav>
