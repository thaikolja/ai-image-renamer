{# Admin Settings Page Template #}
<div class="wrap air-settings">
    <h1>{{ __('AI Image Renamer Settings', 'ai-image-renamer') }}</h1>

    <form
            method="post"
            action="options.php">
        {{ settings_fields(option_group)|raw }}
        {{ do_settings_sections(page_slug)|raw }}
        {{ submit_button(__('Save Settings', 'ai-image-renamer'))|raw }}
    </form>
</div>
